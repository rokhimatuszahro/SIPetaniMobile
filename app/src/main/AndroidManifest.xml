<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.sipetani">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/botani"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/botani"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Tiket"></activity>
        <activity android:name=".Profil" />
        <activity android:name=".DetailPemesanan" />
        <activity android:name=".DetailPembayaran" />
        <activity
            android:name=".BuatAkun"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LupaPassword"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Navigation" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>